This is a file over HTTP and How I did it also some info on HTTP.

HTTP (HyperText Transfer Protocol) is the communication protocol used for transferring
web data between a client (your browser or tool) and a server (website).

In terms to understand easier:
HTTP is the language your computer uses to talk to websites. 

Ubuntu install wireshark. Install wireshark onto your terminal unbutnu, vm wherever you may
have ubuntu installed and you done have to use ubuntu you can use any sort of linux sortware as
long as you have access to install wireshark.

1. What Linux tool was used to execute a file download? 
Answer: wget

2. What is the name of the web server software that handled the request?
Answer: ngnix

3. What IP address initiated request?
Answer: 192.168.1.140

4. What is the IP address of the server?
Answer: 174.143.213.184

5. What is the md5sum of the file downloaded?
Answer: 966007c476e0c200fba8b28b250a6379

Steps to question 5 because I got stuck on it (because I am a noob) lol
1. extract the logo.png in wireshark.
2. To do so go to file then export objects then http
3. save it in whatever folder you were working on
4. go to linux
5. look for that file and type in the command md5sum logo.png ( or whatever you named that file )
6. Then you will get the md5 file name (boom)
